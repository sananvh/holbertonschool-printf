.TH _PRINTF 3 "November 2024" "Custom Library" "User Functions"
.SH NAME
_printf \- produces output according to a format

.SH SYNOPSIS
.B #include "main.h"
.BI "int _printf(const char *format, ...);"

.SH DESCRIPTION
The
.B _printf
function produces output according to a
.IR format .
The format string is composed of zero or more directives. These directives are either literal characters to be printed or conversion specifiers, which determine the type of argument to be printed.

.TP
The following conversion specifiers are supported:
.BR c
Prints a single character.
.BR s
Prints a string of characters.
.BR %
Prints a literal percent sign.
.BR d , i
Prints a signed decimal integer.

.SH RETURN VALUE
The
.B _printf
function returns the number of characters printed (excluding the null byte).

If the
.I format
string is NULL, the function returns \-1.

.SH EXAMPLES
To print a string, character, and integer:

.nf
.B #include "main.h"
.BI "int main(void)"
{
    _printf("Character: %c\\n", 'A');
    _printf("String: %s\\n", "Hello, world!");
    _printf("Integer: %d\\n", 42);
    return (0);
}
.fi

.SH NOTES
The
.B _printf
function does not support:
- Field width
- Precision
- Length modifiers
- Flag characters

.SH SEE ALSO
.B printf(3),
.B write(2)

.SH AUTHOR
Written by [Your Name].

